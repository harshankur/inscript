import React from 'react'
import ReactDOM from 'react-dom/client'
import InscriptApp from './app.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <InscriptApp />
    </React.StrictMode>,
)
